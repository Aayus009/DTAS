$kind: http-request
name: API-05 TransparencySync.ashx refuses anonymous
url: "{{baseUrl}}/Modules/Transparency/TransparencySync.ashx"
method: GET
scripts:
  - type: afterResponse
    code: |-
      var t = pm.response.text().toLowerCase();
      pm.test('API-05 TransparencySync refuses anonymous caller', function () { pm.expect(t).to.include('sign in'); });
    language: text/javascript
order: 5000
